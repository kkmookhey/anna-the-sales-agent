# deps & build
node_modules/
dist/
cdk.out/
ni-sales-agent/aws/skills/

# large/binary brand assets (referenced at build time, not tracked)
assets/*.pptx

# secrets — NEVER commit
.env
.env.*
*.env
!.env.example
App Password.html
*App Password*.html

# session memory & os cruft
.remember/
*.DS_Store
.DS_Store
*.zip

# Generated proposal artifacts and cowork ledger (not source)
ni-sales-agent/proposals/
ni-sales-agent/cowork-state/
